{
  "files": [
    {
      "path": "src/services/appointmentService.ts",
      "content": "export interface Appointment {\n  id: string;\n  title: string;\n  startTime: string; // ISO string\n  endTime: string; // ISO string\n  recurringPattern?: RecurringPattern;\n}\n\nexport interface RecurringPattern {\n  frequency: 'daily' | 'weekly' | 'monthly';\n  interval: number; // every N days/weeks/months\n  daysOfWeek?: number[]; // 0=Sun, 1=Mon, ... 6=Sat (only for weekly)\n  endDate?: string; // ISO date (optional)\n  occurrences?: number; // number of occurrences (optional)\n}\n\nexport interface ConflictResult {\n  hasConflict: boolean;\n  conflictingAppointments: Appointment[];\n  message?: string;\n}\n\nexport interface CheckConflictParams {\n  startTime: string;\n  endTime: string;\n  existingAppointments: Appointment[];\n  bufferMinutes?: number; // default 30\n  checkRecurring?: boolean; // default true\n  appointmentId?: string; // skip conflict with self when editing\n}\n\nfunction parseISO(iso: string): Date {\n  return new Date(iso);\n}\n\nfunction addMinutes(date: Date, minutes: number): Date {\n  return new Date(date.getTime() + minutes * 60000);\n}\n\nfunction isBefore(a: Date, b: Date): boolean {\n  return a.getTime() < b.getTime();\n}\n\nfunction isAfter(a: Date, b: Date): boolean {\n  return a.getTime() > b.getTime();\n}\n\nfunction addDays(date: Date, days: number): Date {\n  const result = new Date(date);\n  result.setDate(result.getDate() + days);\n  return result;\n}\n\nfunction addMonths(date: Date, months: number): Date {\n  const result = new Date(date);\n  result.setMonth(result.getMonth() + months);\n  return result;\n}\n\nfunction timesOverlapWithBuffer(\n  newStart: Date,\n  newEnd: Date,\n  existingStart: Date,\n  existingEnd: Date,\n  bufferMinutes: number\n): boolean {\n  const bufferedExistingStart = addMinutes(existingStart, -bufferMinutes);\n  const bufferedExistingEnd = addMinutes(existingEnd, bufferMinutes);\n  return isBefore(newStart, bufferedExistingEnd) && isAfter(newEnd, bufferedExistingStart);\n}\n\nfunction expandRecurringAppointments(\n  appointment: Appointment,\n  rangeStart: Date,\n  rangeEnd: Date,\n  bufferMinutes: number\n): Array<{ start: Date; end: Date }> {\n  const occurrences: Array<{ start: Date; end: Date }> = [];\n  const pattern = appointment.recurringPattern!;\n  const originalStart = parseISO(appointment.startTime);\n  const originalEnd = parseISO(appointment.endTime);\n  const duration = originalEnd.getTime() - originalStart.getTime();\n\n  const searchStart = addMinutes(rangeStart, -bufferMinutes);\n  const searchEnd = addMinutes(rangeEnd, bufferMinutes);\n\n  const maxGenerate = 365;\n  let currentDate = new Date(originalStart);\n  let count = 0;\n\n  while (count < maxGenerate) {\n    const occStart = new Date(currentDate);\n    const occEnd = new Date(occStart.getTime() + duration);\n\n    if (isAfter(occEnd, searchStart) && isBefore(occStart, searchEnd)) {\n      occurrences.push({ start: occStart, end: occEnd });\n    }\n\n    if (pattern.frequency === 'daily') {\n      currentDate = addDays(currentDate, pattern.interval);\n    } else if (pattern.frequency === 'weekly') {\n      currentDate = addDays(currentDate, 7 * pattern.interval);\n    } else if (pattern.frequency === 'monthly') {\n      currentDate = addMonths(currentDate, pattern.interval);\n    }\n\n    if (pattern.endDate && isAfter(currentDate, parseISO(pattern.endDate))) break;\n    if (pattern.occurrences && count >= pattern.occurrences) break;\n    count++;\n  }\n\n  return occurrences;\n}\n\n/**\n * Checks for conflicts with existing appointments, including buffer time and recurring.\n */\nexport function checkAppointmentConflict({\n  startTime,\n  endTime,\n  existingAppointments,\n  bufferMinutes = 30,\n  checkRecurring = true,\n  appointmentId,\n}: CheckConflictParams): ConflictResult {\n  const conflicts: Appointment[] = [];\n  const newStart = parseISO(startTime);\n  const newEnd = parseISO(endTime);\n\n  for (const appt of existingAppointments) {\n    if (appointmentId && appt.id === appointmentId) continue;\n\n    if (!appt.recurringPattern) {\n      const apptStart = parseISO(appt.startTime);\n      const apptEnd = parseISO(appt.endTime);\n      if (timesOverlapWithBuffer(newStart, newEnd, apptStart, apptEnd, bufferMinutes)) {\n        conflicts.push(appt);\n      }\n    } else if (checkRecurring) {\n      const expanded = expandRecurringAppointments(appt, newStart, newEnd, bufferMinutes);\n      for (const expandedAppt of expanded) {\n        if (timesOverlapWithBuffer(newStart, newEnd, expandedAppt.start, expandedAppt.end, bufferMinutes)) {\n          conflicts.push(appt);\n          break;\n        }\n      }\n    }\n  }\n\n  return {\n    hasConflict: conflicts.length > 0,\n    conflictingAppointments: conflicts,\n    message: conflicts.length > 0 ? 'Appointment conflicts with existing appointments' : undefined,\n  };\n}\n"
    },
    {
      "path": "src/screens/AppointmentScreen.tsx",
      "content": "import React, { useState, useEffect } from 'react';\nimport { View, Text, TextInput, Button, Alert } from 'react-native';\nimport { checkAppointmentConflict, Appointment, ConflictResult } from '../services/appointmentService';\n\ninterface AppointmentScreenProps {\n  existingAppointments: Appointment[];\n  onSave: (appointment: Appointment) => void;\n  initialAppointment?: Appointment;\n}\n\nconst AppointmentScreen: React.FC<AppointmentScreenProps> = ({ existingAppointments, onSave, initialAppointment }) => {\n  const [title, setTitle] = useState(initialAppointment?.title || '');\n  const [startTime, setStartTime] = useState(initialAppointment?.startTime || '');\n  const [endTime, setEndTime] = useState(initialAppointment?.endTime || '');\n  const [conflict, setConflict] = useState<ConflictResult | null>(null);\n\n  useEffect(() => {\n    if (startTime && endTime) {\n      const filteredAppointments = initialAppointment\n        ? existingAppointments.filter(a => a.id !== initialAppointment.id)\n        : existingAppointments;\n      const result = checkAppointmentConflict({\n        startTime,\n        endTime,\n        existingAppointments: filteredAppointments,\n        bufferMinutes: 30,\n        checkRecurring: true,\n      });\n      setConflict(result);\n    } else {\n      setConflict(null);\n    }\n  }, [startTime, endTime, existingAppointments, initialAppointment]);\n\n  const handleSave = () => {\n    if (conflict && conflict.hasConflict) {\n      Alert.alert(\n        'Conflict Detected',\n        `This appointment conflicts with: ${conflict.conflictingAppointments.map(a => a.title).join(', ')}. Do you want to proceed?`,\n        [\n          { text: 'Cancel', style: 'cancel' },\n          { text: 'Save Anyway', onPress: () => saveAppointment() }\n        ]\n      );\n    } else {\n      saveAppointment();\n    }\n  };\n\n  const saveAppointment = () => {\n    const appointment: Appointment = {\n      id: initialAppointment?.id || Date.now().toString(),\n      title,\n      startTime,\n      endTime,\n    };\n    onSave(appointment);\n  };\n\n  return (\n    <View>\n      <Text>Title</Text>\n      <TextInput value={title} onChangeText={setTitle} />\n      <Text>Start Time (ISO)</Text>\n      <TextInput value={startTime} onChangeText={setStartTime} placeholder=\"YYYY-MM-DDTHH:mm:ss.sssZ\" />\n      <Text>End Time (ISO)</Text>\n      <TextInput value={endTime} onChangeText={setEndTime} placeholder=\"YYYY-MM-DDTHH:mm:ss.sssZ\" />\n      {conflict && conflict.hasConflict && (\n        <View style={{ backgroundColor: 'red', padding: 8 }}>\n          <Text style={{ color: 'white' }}>Conflict: {conflict.message}</Text>\n          <Text style={{ color: 'white' }}>\n            With: {conflict.conflictingAppointments.map(a => a.title).join(', ')}\n          </Text>\n        </View>\n      )}\n      <Button title=\"Save\" onPress={handleSave} disabled={!title || !startTime || !endTime} />\n    </View>\n  );\n};\n\nexport default AppointmentScreen;\n"
    },
    {
      "path": "src/__tests__/appointmentService.test.ts",
      "content": "import { checkAppointmentConflict, Appointment } from '../services/appointmentService';\n\ndescribe('checkAppointmentConflict', () => {\n  const baseAppointments: Appointment[] = [\n    { id: '1', title: 'Meeting', startTime: '2023-10-01T10:00:00.000Z', endTime: '2023-10-01T11:00:00.000Z' },\n    { id: '2', title: 'Lunch', startTime: '2023-10-01T12:00:00.000Z', endTime: '2023-10-01T13:00:00.000Z' },\n    { id: '3', title: 'Recurring Weekly', startTime: '2023-10-02T09:00:00.000Z', endTime: '2023-10-02T10:00:00.000Z', recurringPattern: { frequency: 'weekly', interval: 1 } },\n  ];\n\n  it('detects exact same time conflict', () => {\n    const result = checkAppointmentConflict({\n      startTime: '2023-10-01T10:00:00.000Z',\n      endTime: '2023-10-01T11:00:00.000Z',\n      existingAppointments: baseAppointments,\n    });\n    expect(result.hasConflict).toBe(true);\n  });\n\n  it('detects overlapping appointments', () => {\n    const result = checkAppointmentConflict({\n      startTime: '2023-10-01T10:30:00.000Z',\n      endTime: '2023-10-01T11:30:00.000Z',\n      existingAppointments: baseAppointments,\n    });\n    expect(result.hasConflict).toBe(true);\n  });\n\n  it('detects buffer violation (less than 30 min gap)', () => {\n    const result = checkAppointmentConflict({\n      startTime: '2023-10-01T11:15:00.000Z',\n      endTime: '2023-10-01T12:00:00.000Z',\n      existingAppointments: baseAppointments,\n      bufferMinutes: 30,\n    });\n    expect(result.hasConflict).toBe(true);\n  });\n\n  it('does not flag non-overlapping with sufficient buffer', () => {\n    const result = checkAppointmentConflict({\n      startTime: '2023-10-01T11:30:00.000Z',\n      endTime: '2023-10-01T12:00:00.000Z',\n      existingAppointments: baseAppointments,\n      bufferMinutes: 30,\n    });\n    expect(result.hasConflict).toBe(false);\n  });\n\n  it('detects conflict with recurring appointment on same day', () => {\n    const result = checkAppointmentConflict({\n      startTime: '2023-10-02T09:30:00.000Z',\n      endTime: '2023-10-02T10:00:00.000Z',\n      existingAppointments: baseAppointments,\n    });\n    expect(result.hasConflict).toBe(true);\n  });\n\n  it('does not conflict with recurring on different day', () => {\n    const result = checkAppointmentConflict({\n      startTime: '2023-10-03T09:00:00.000Z',\n      endTime: '2023-10-03T10:00:00.000Z',\n      existingAppointments: baseAppointments,\n    });\n    expect(result.hasConflict).toBe(false);\n  });\n\n  it('respects custom buffer time', () => {\n    const result = checkAppointmentConflict({\n      startTime: '2023-10-01T11:10:00.000Z',\n      endTime: '2023-10-01T11:30:00.000Z',\n      existingAppointments: baseAppointments,\n      bufferMinutes: 15,\n    });\n    expect(result.hasConflict).toBe(true);\n  });\n\n  it('returns false for no conflict', () => {\n    const result = checkAppointmentConflict({\n      startTime: '2023-10-01T14:00:00.000Z',\n      endTime: '2023-10-01T15:00:00.000Z',\n      existingAppointments: baseAppointments,\n    });\n    expect(result.hasConflict).toBe(false);\